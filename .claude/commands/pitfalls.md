Get known pitfalls for this project, ranked by severity. These are problems that previous sessions encountered, with resolution strategies.

Run the following command and present the results to the user:

```bash
CMM_PROJECT_ID="${CMM_PROJECT_ID}" CMM_STORE_PATH="${CMM_STORE_PATH}" cd "/Users/sazankhalid/Downloads/cmm/cognitive-memory" && source "/Users/sazankhalid/Downloads/cmm/.claude/cmm-env.sh" && "$CMM_PYTHON" -m src.delivery.cli_query pitfalls
```

After presenting the results, highlight which pitfalls are most relevant to the current task or files being worked on.
